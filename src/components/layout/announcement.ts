/**
 * Height of the reviews announcement bar in px. Shared between the bar itself
 * and the Navbar, which offsets its fixed position by this amount while the
 * bar is still on screen.
 */
export const ANNOUNCEMENT_BAR_HEIGHT = 40;
